const initialState = {
    name:'',
    email: '',
    password: '',

}

const adminReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default adminReducer;